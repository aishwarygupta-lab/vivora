// Tailwind CSS v4 — the '@tailwindcss/postcss' plugin replaces the old
// 'tailwindcss' + 'autoprefixer' pair (autoprefixing is built in).
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
